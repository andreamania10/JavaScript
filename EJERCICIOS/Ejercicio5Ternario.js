//EJERCICIO 1. Declare una constante numérica ‘b’ y asignale un valor. Mostrar una traza indicando si el valor de ‘b’ es positivo o negativo. Consideraremos el 0 como positivo. Utiliza el operador condicional (? : ) dentro del console.log para resolverlo.

const b = 10;
console.log(b < 0 ? 'es un número negativo' : 'es un número positivo')

//EJERCICIO 2: Declarar una constante numérica ‘i’ y asignale un valor. Muestra un mensaje indicando si ‘i’ es par o impar. Utiliza el operador condicional ( ? : ) dentro del console.log para resolverlo.

const i = 10;
console.log(i % 2 === 0 ? 'es par' : 'es impar')